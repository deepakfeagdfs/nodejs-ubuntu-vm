version: "3.8"
services:
  app:
    image: node:20-bookworm
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && node server.js"
    ports:
      - "7860:7860"
    environment:
      PORT: "7860"
    restart: unless-stopped
